generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Canva {
  id         String   @id @default(uuid())
  name       String
  fileUrl    String
  latestDate DateTime @default(now())
}
